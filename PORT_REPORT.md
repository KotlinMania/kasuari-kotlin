# Port report — `kasuari-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `kasuari`
- **Codex-kotlin distance**: **hop 2** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 9 `use` sites from 1 other repos
- **Outbound**: 1 `use` sites to 1 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `kasuari` | `kasuari` | 9 | 1 | COD-207 | 298 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (9 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `ratatui-kotlin` | `ratatui-core` | 9 |  |

### Symbol-level demand (8 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `kasuari::Strength` | 2 | 0 | 2 | 1 |
| `kasuari::AddConstraintError` | 1 | 0 | 1 | 1 |
| `kasuari::Expression` | 1 | 0 | 1 | 1 |
| `kasuari::Solver` | 1 | 0 | 1 | 1 |
| `kasuari::Variable` | 1 | 0 | 1 | 1 |
| `kasuari::WeightedRelation::EQ` | 1 | 0 | 1 | 1 |
| `kasuari::WeightedRelation::GE` | 1 | 0 | 1 | 1 |
| `kasuari::WeightedRelation::LE` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 8 symbols)

##### `kasuari::Strength` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:11` — `use kasuari::Strength`
- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:1287` — `use kasuari::Strength`

##### `kasuari::AddConstraintError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:11` — `use kasuari::AddConstraintError`

##### `kasuari::Expression` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:11` — `use kasuari::Expression`

##### `kasuari::Solver` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:11` — `use kasuari::Solver`

##### `kasuari::Variable` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:11` — `use kasuari::Variable`

##### `kasuari::WeightedRelation::EQ` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:10` — `use kasuari::WeightedRelation::EQ`

##### `kasuari::WeightedRelation::GE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:10` — `use kasuari::WeightedRelation::GE`

##### `kasuari::WeightedRelation::LE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ratatui-kotlin/ratatui-core` `src/layout/layout.rs:10` — `use kasuari::WeightedRelation::LE`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `thiserror-kotlin` | 1 | 1 | `thiserror` | COD-153 | 11 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `hashbrown` | 66 | 17 | `kasuari` |
| `rstest` | 53 | 2 | `kasuari` |
| `portable-atomic` | 10 | 7 | `kasuari` |
| `portable-atomic-util` | 2 | 1 | `kasuari` |
